{
  "name": "coffee-machine-test",
  "version": "0.0.1",
  "main": "src/app.js",
  "scripts": {
    "start": "ts-node-dev --respawn src/app.ts",
    "pretest": "npm install",
    "test": "mocha -r ts-node/register tests/**/*.test.ts",
    "report": "mocha -R mocha-junit-reporter -r ts-node/register tests/**/*.test.ts"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/chai": "^4.2.12",
    "@types/mocha": "^8.0.3",
    "@types/node": "^14.11.2",
    "chai": "^4.2.0",
    "mocha": "^8.1.3",
    "mocha-junit-reporter": "^2.0.0",
    "ts-node": "^9.0.0",
    "ts-node-dev": "^1.0.0-pre.63",
    "typescript": "^4.0.3"
  }
}
